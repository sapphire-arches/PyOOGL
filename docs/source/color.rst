Color
=====

The ``color`` module provides utilities for converting between color spaces and working with colors.

.. py:module:: PyOOGL.color

.. py:function:: hsv_to_rgb(h, s, v)

    This function accepts both 3 arguments, tuples, and lists
    Converts HSV color to RGB color in the 0-1 float format
